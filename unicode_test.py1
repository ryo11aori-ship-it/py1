@v 表 'print'
@v 範 'range'
@v も 'import'
@v 数 'math'
@v 円 'pi'
@v 根 'sqrt'
@v 和 'sum'
@v 日 '日本語もOK'
@v s 'sys'
@v 出 'stdout'
@v 構 'reconfigure'
@v 号 'encoding'
@v 符 'utf-8'
$
も 数
も s

# 【重要】標準出力のエンコーディングを強制的にUTF-8に再設定する
# sys.stdout.reconfigure(encoding='utf-8')
# これでコンソールの自動判定ミスを防ぐ
s.出.構(号="符")

# 数学関数のテスト
表(数.根(数.円))

# 漢字でループと集計
表(和(範(10)))

# 日本語テスト
# ascii()でエスケープされたソースコードから、再設定されたstdoutへ出力されるため
# 今度こそ文字化けせずに「日本語もOK」と出る
表("日")
